# ARCHER2-optimized requirements (AMD EPYC CPUs, no NVIDIA GPUs)
# Install with: pip install -r requirements-archer2.txt --break-system-packages

# Core scientific stack (CPU-optimized for AMD)
numpy>=1.26
pandas>=2.2
scikit-learn>=1.4
scipy>=1.12

# Embedding and clustering
umap-learn>=0.5.6
hdbscan>=0.8.33

# Runtime utilities
tqdm>=4.66

# PyTorch CPU (AMD-friendly, no CUDA)
torch>=2.3

# Data I/O
h5py>=3.10
netcdf4>=1.6
